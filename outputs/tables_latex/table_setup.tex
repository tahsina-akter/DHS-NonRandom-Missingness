% Consistent spacing in all tables (optional)
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{6pt}

% Helper for significance stars if needed later
\newcommand{\sym}[1]{\ifmmode^{#1}\else\(^{#1}\)\fi}